import { PathReplacePipe } from './path-replace.pipe';

describe('PathReplacePipe', () => {
  it('create an instance', () => {
    const pipe = new PathReplacePipe();
    expect(pipe).toBeTruthy();
  });
});
